#ifndef VECTOR_H
#define VECTOR_H

int init(char *nombre, int N);    // Inicializa un vector
int set(char *nombre, int i, int v);  // Asigna un valor en la posición i
int get(char *nombre, int i, int *x); // Obtiene el valor en la posición i

#endif
